MODULE TestWhile2;

VAR 
  x : INTEGER; y : INTEGER;
  a : INTEGER; b : INTEGER; c : INTEGER; d : INTEGER;

BEGIN
  x := 37; y := 0;
  a := 1; b := 2; c := 3; d := 3;
  WHILE (a < b) & (c < d) OR (a < c) & (y < 10) DO
    x := x + 3; y := y + 1
  END
  (* x = 67 ('C') *)
END TestWhile2.
