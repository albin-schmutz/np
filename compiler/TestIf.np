MODULE TestIf;

VAR 
  x : INTEGER; y : INTEGER;
  a : INTEGER; b : INTEGER; c : INTEGER; d : INTEGER;

BEGIN
  x := 65; y := 0;
  a := 1; b := 2; c := 3; d := 3;
  IF (a < b) & (c < d) OR (a < c) & (y < 10) THEN
    x := x + 3; y := y + 1
  END
  (* x = 68 ('D') *)
END TestIf.
